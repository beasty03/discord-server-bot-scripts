# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# ── Environment ───────────────────────────────────────────────────────────────
.env
.env.*
config.json
config.yaml
config.yml
*.secret

# /set_mc_server can persist a raw Client API key here — never commit it
Webhooks/minecraft_server/mc_servers_config.json

# ── DLC — guides and templates (dev tools, not live content) ──────────────────
DND_DLC/CREATOR_GUIDE.md
DND_DLC/_template/
DND_DLC/campaigns/_template/

# ── Claude Code project files ─────────────────────────────────────────────────
.claude/
CLAUDE.md

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
